# Astra Voxel World (vendored)

This MIT-licensed crate is vendored from the local Astra Imperium workspace so
the game can build without absolute paths or access to another checkout. Only
the deterministic generator, voxel edit model, and mesh renderer used by the
game are included.